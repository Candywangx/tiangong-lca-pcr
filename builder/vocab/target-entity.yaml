schema_version: 1
vocab: target_entity
values:
  flow:
    description: PCR identity and reference-flow requirements project to flow records.
  process:
    description: PCR guidance supports foreground process data production.
  lifecyclemodel:
    description: PCR guidance supports downstream lifecycle model projections.
  dataset:
    description: PCR guidance supports foreground data package and published dataset projections.
